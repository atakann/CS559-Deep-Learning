Author: Atakan Serbes

-Homework report is given as a .pdf file
-Network is constructed in an ipython notebook and run on google colab during the experiments. The ipython notebook for the network is provided. 
-The python code for extracting the images and their labels and saving them as numpy arrays is also given in the contents (Img2NpArr, Img2NParrayVal, Img2NpArrTest). 
-The output of the python scripts are given as .npy files (images and their labels) which are called and used in the main ipython notebook.

